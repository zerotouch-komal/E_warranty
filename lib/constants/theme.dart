import 'package:flutter/material.dart';

const darkColor =
    LinearGradient(colors: [Colors.blue, Colors.white]);

const whiteColor = Colors.white;

const themeapp = Colors.blue; 

const themeBlack = Colors.white;

const themeGray = Color(0xFF333333);